name: PMSCore API CI

on:
  workflow_dispatch:

  push:
    branches:
      - S_21_P2
    paths:
      - 'src/PMSCoreAPI/**'
      - 'config/patch.config'

permissions:
  contents: write

env:
  CONFIG_FILE: config/patch.config

jobs:

  read_config:
    name: Read Patch Configuration
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    outputs:
      sprint: ${{ steps.config.outputs.sprint }}
      patch: ${{ steps.config.outputs.patch }}
      version: ${{ steps.config.outputs.version }}
      timestamp: ${{ steps.config.outputs.timestamp }}

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - id: config
        shell: bash
        run: |
          echo "sprint=$(grep '^SPRINT=' $CONFIG_FILE | cut -d '=' -f2)" >> $GITHUB_OUTPUT
          echo "patch=$(grep '^PATCH=' $CONFIG_FILE | cut -d '=' -f2)" >> $GITHUB_OUTPUT
          echo "version=$(grep '^VERSION=' $CONFIG_FILE | cut -d '=' -f2)" >> $GITHUB_OUTPUT
          echo "timestamp=$(TZ=Asia/Kolkata date +"%Y%m%d_%H%M%S")" >> $GITHUB_OUTPUT

      - name: Display Configuration
        run: |
          echo "Sprint : ${{ steps.config.outputs.sprint }}"
          echo "Patch  : ${{ steps.config.outputs.patch }}"
          echo "Version: ${{ steps.config.outputs.version }}"

  build:
    name: Build PMSCore API
    needs: read_config
    runs-on: ubuntu-latest

    env:
      SPRINT: ${{ needs.read_config.outputs.sprint }}
      PATCH: ${{ needs.read_config.outputs.patch }}
      VERSION: ${{ needs.read_config.outputs.version }}

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/PMSCoreAPI/PMSCoreAPI.csproj

      - name: Build
        run: dotnet build src/PMSCoreAPI/PMSCoreAPI.csproj --configuration Release --no-restore

      - name: Test
        run: dotnet test src/PMSCoreAPI/PMSCoreAPI.csproj --no-build --verbosity normal

      - name: Publish
        run: |
          dotnet publish src/PMSCoreAPI/PMSCoreAPI.csproj \
          -c Release \
          -o publish/PMSCoreAPI

      - name: Verify Publish Output
        run: ls -R publish/PMSCoreAPI

      - name: Upload Publish Files
        uses: actions/upload-artifact@v4
        with:
          name: publish-files-${{ needs.read_config.outputs.timestamp }}
          path: publish/PMSCoreAPI

  artifact_management:
    name: Rotate & Store Artifacts
    needs:
      - read_config
      - build

    concurrency:
      group: pmscore-api-artifact-${{ github.ref }}
      cancel-in-progress: false

    runs-on: ubuntu-latest

    env:
      SPRINT: ${{ needs.read_config.outputs.sprint }}
      PATCH: ${{ needs.read_config.outputs.patch }}

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Download Publish Files
        uses: actions/download-artifact@v4
        with:
          name: publish-files-${{ needs.read_config.outputs.timestamp }}
          path: publish/PMSCoreAPI

      - name: Install Zip
        run: |
          sudo apt-get update
          sudo apt-get install -y zip

      - name: Ensure Artifact Folder Structure
        shell: bash
        run: |
          mkdir -p artifacts/PMSCoreAPI/New
          mkdir -p artifacts/PMSCoreAPI/Old1
          mkdir -p artifacts/PMSCoreAPI/Old2

      - name: Configure Git
        shell: bash
        run: |
          git config user.name "sam"
          git config user.email "sam@github-actions.local"
          git checkout ${GITHUB_REF_NAME}

      - name: Sync With Remote
        shell: bash
        run: |
          # This job runs one-at-a-time (see concurrency group above), so
          # this is guaranteed to be the latest state before we start
          # rotating/committing. We never rebase after this point, so binary
          # zip renames can never conflict.
          git fetch origin ${GITHUB_REF_NAME}
          git reset --hard origin/${GITHUB_REF_NAME}

      - name: Capture Previous Commit Messages
        shell: bash
        run: |
          NEW_MSG=$(git log -1 --format=%s -- artifacts/PMSCoreAPI/New 2>/dev/null)
          OLD1_MSG=$(git log -1 --format=%s -- artifacts/PMSCoreAPI/Old1 2>/dev/null)

          [ -z "$NEW_MSG" ] && NEW_MSG="No previous artifact"
          [ -z "$OLD1_MSG" ] && OLD1_MSG="No previous artifact"

          echo "NEW_MSG=$NEW_MSG" >> $GITHUB_ENV
          echo "OLD1_MSG=$OLD1_MSG" >> $GITHUB_ENV

      - name: Rotate Existing Artifacts
        shell: bash
        run: |
          # dotglob so a folder holding only a hidden file (e.g. .gitkeep)
          # still matches '*' the same way `ls -A` above already treats it
          # as non-empty; nullglob so an unmatched '*' expands to nothing
          # instead of being passed to mv as a literal, unresolvable path.
          shopt -s nullglob dotglob

          echo "Clearing Old2"
          rm -rf artifacts/PMSCoreAPI/Old2/*

          echo "Moving Old1 -> Old2"
          if [ "$(ls -A artifacts/PMSCoreAPI/Old1 2>/dev/null)" ]; then
            mv artifacts/PMSCoreAPI/Old1/* artifacts/PMSCoreAPI/Old2/
          fi

          echo "Moving New -> Old1"
          if [ "$(ls -A artifacts/PMSCoreAPI/New 2>/dev/null)" ]; then
            mv artifacts/PMSCoreAPI/New/* artifacts/PMSCoreAPI/Old1/
          fi

      - name: Commit Old2 Rotation
        shell: bash
        run: |
          git add artifacts/PMSCoreAPI/Old2
          if git diff --cached --quiet; then
            echo "No Old2 changes."
          else
            git commit -m "Old2 <= ${OLD1_MSG}"
          fi

      - name: Commit Old1 Rotation
        shell: bash
        run: |
          git add artifacts/PMSCoreAPI/Old1
          if git diff --cached --quiet; then
            echo "No Old1 changes."
          else
            git commit -m "Old1 <= ${NEW_MSG}"
          fi

      - name: Create Latest Artifact
        shell: bash
        run: |
          TIMESTAMP=$(TZ=Asia/Kolkata date +"%Y%m%d_%H%M%S")
          ARTIFACT_NAME="PMSCore_Sprint${SPRINT}_Patch${PATCH}_${TIMESTAMP}.zip"
          echo "ARTIFACT_NAME=$ARTIFACT_NAME" >> $GITHUB_ENV

          zip -r \
            artifacts/PMSCoreAPI/New/${ARTIFACT_NAME} \
            publish/PMSCoreAPI

      - name: Verify Artifact Structure
        run: ls -R artifacts/PMSCoreAPI

      - name: Commit New Artifact
        shell: bash
        run: |
          git add artifacts/PMSCoreAPI/New
          if git diff --cached --quiet; then
            echo "No New artifact changes."
            exit 0
          fi

          TIMESTAMP=$(TZ=Asia/Kolkata date +"%Y-%m-%d %H:%M:%S IST")
          git commit -m "PMSCoreAPI Artifact | Sprint ${SPRINT} | Patch ${PATCH} | ${TIMESTAMP}"

      - name: Push Commits
        shell: bash
        run: |
          # No rebase here on purpose: rebasing replays commits that rename
          # binary zip files, which Git cannot auto-merge. The concurrency
          # group plus "Sync With Remote" already guarantee we're pushing on
          # top of the true latest commit, so a plain push should always be
          # a fast-forward. We still retry a few times for transient network
          # issues only.
          for i in 1 2 3
          do
            if git push origin ${GITHUB_REF_NAME}; then
              echo "Push Successful"
              exit 0
            fi
            echo "Push rejected or failed. Fetching latest and retrying ($i)..."
            git fetch origin ${GITHUB_REF_NAME}
            sleep 2
          done

          echo "Push failed after 3 attempts. Another run may have written to the branch outside this workflow's concurrency group."
          exit 1

      - name: Upload GitHub Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PMSCoreAPI-Latest-${{ needs.read_config.outputs.timestamp }}
          path: artifacts/PMSCoreAPI/New/${{ env.ARTIFACT_NAME }}
          retention-days: 30
          if-no-files-found: error
